fn main(): int {
    let a: int = 1 + 2 * 3 - 4 / 2 % 3;
    let b: bool = true && false || true;
    let c: bool = 1 == 1;
    let d: bool = 1 != 2;
    let e: bool = 1 < 2;
    let f: bool = 2 > 1;
    let g: bool = 1 <= 2;
    let h: bool = 2 >= 1;
    let i: int = -a;
    let j: bool = !b;
    return a;
}
