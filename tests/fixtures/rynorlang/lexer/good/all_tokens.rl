fn add(a: int, b: int) -> int {
    let total = a + b * 2 / 3 % 5 - 1;
    if !false && total <= 50 && true {
        return total;
    } else {
        while total >= 0 || false {
            let equal = total == 42;
            let different = total != 7;
            let lower = total < 100;
            let higher = total > 0;
        }
    }
    let text: str = "Rynor\nOS\t\"\\";
    let flag: bool = false;
    call(text, flag);
}
