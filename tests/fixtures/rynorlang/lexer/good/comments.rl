// comment at start
// this is a comment
let x = 1; // trailing comment
// comment with symbols == != && || -> {} () ; , :
let y = 2;
//
let z = 3; // comment after code
// comment with unicode? only ascii here
//   spaces and	tabs in comment
let a = 4;//no space before comment
let b = 5; // comment with "quotes" and // nested
// /* block comment markers are just text since block comments excluded */
// // multiple slashes ////
let c = 6;
