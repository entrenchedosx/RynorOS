fn main() {
    let a: int = 1 + 2 * 3;
    let b: bool = true && false || true;
    let c: bool = true == false;
    let d: int = -5 * 2;
    let e: int = 1 * 2 + 3;
    let f: int = 10 - 2 / 2;
    let g: bool = true;
    let h: int = -10;
    let i: int = 1 + 2 * 3 % 4;
    let j: bool = true && false;
    let k: int = -10 * 2;
}
