fn main() {
    let a: int = 10 - 5 - 2;
    let b: int = 20 / 2 * 3;
    let c: int = 1 + 2 + 3 + 4;
    let d: int = 100 % 30 % 7;
    let e: bool = true && false && true;
    let f: bool = true || false || true;
    let g: bool = 1 == 2 != 3;
    let h: int = 10 * 2 / 5;
    let i: int = 1 - 2 + 3 - 4;
}
